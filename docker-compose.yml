version: '3'
services:

  postgresql:
    container_name: postgresql
    environment:
      - POSTGRES_PASSWORD=Rahasia
      - POSTGRES_DB=toko_mandiri
    networks:
      - app-network
    volumes:
      - "postgres-data:/var/lib/postgresql/data"
    image: postgres

  app-toko-mandiri:
    container_name: app-toko-mandiri2
    environment:
      - HOST_DB=postgresql
      - PORT_DB=5432
      - APP_PORT=8090
      - DB_NAME=toko_mandiri
      - USERNAME_DB=postgres
      - PASSWORD_DB=Rahasia
    ports:
      - "8085:8090"
    build: .
    networks:
      - app-network

volumes:
  postgres-data:
networks:
  app-network:
    driver: bridge




#version: '3'
#
#services:
#  springapp:
#    image: my-spring
#    build:
#      context: ./springapp
#      dockerfile: Dockerfile
#    ports:
#      - "8088:8090"
#    depends_on:
#      - database
#    environment:
#      APP_PORT: 8090
#      HOST_DB: database
#      PORT_DB: 5432
#      DB_NAME: mydb
#      USERNAME_DB: postgres
#      PASSWORD_DB: examplepassword
#    networks:
#      - mynetwork
#
#  database:
#    image: postgres:latest
#    environment:
#      POSTGRES_DB: mydb
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: examplepassword
#    ports:
#      - "5435:5432"
#    networks:
#      - mynetwork
#
#networks:
#  mynetwork:
